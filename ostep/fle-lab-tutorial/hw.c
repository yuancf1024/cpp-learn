/*头文件放在这里*/
/*请注意，C 注释包含在斜杠和星号内，并且可能会换行 ?*/
// 如果您使用 gcc，两个斜线也可以使用（并且可能是首选） 
#include <stdio.h>

// 主函数返回一个整数
int main(int argc, char *argv[]) {
    // printf 是一个输出函数
    // 默认情况下，写入标准输出 
    // printf 返回一个整数，但我们忽略它 
    printf("hello, world\n");
    // 返回0表示一切顺利
    return (0);
}

#include <bits/stdc++.h>

using namespace std;

const int MAX_N = 50;
int n, m, k[MAX_N];

// 核心代码，自定义函数
void solve() {
    
}

int main() {
    int n, m, k[MAX_N];
    // 从标准输入读入
    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; i++) {
        scanf("%d", &k[i]);
    }

    // 输出到标准输出
    // if (solve) puts("Yes");
    // else puts("No");
    solve();

    return 0;
}
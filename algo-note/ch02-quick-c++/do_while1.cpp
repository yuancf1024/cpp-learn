#include <cstdio>

int main() { 
    int n;
    scanf("%d", &n);
    do {
        printf("1");
        // sum += n;
        // n++;
        n--;
    } while (n > 0);
    // printf("sum = %d\n", sum);
    return 0;
}